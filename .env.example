# Supabase – used for both server-side rendering and client-side data access.
# The anon key is safe to expose publicly (it is protected by row-level security).
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
